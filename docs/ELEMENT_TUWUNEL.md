# Notes

Production path: publish `BRIDGE_PORT` / `ELEMENT_PORT` and reverse-proxy them —
see [SETUP.md](../SETUP.md).

Element’s homeserver `base_url` must stay the **public bridge** URL
(`PUBLIC_BASE_URL`), never a direct tuwunel address.
